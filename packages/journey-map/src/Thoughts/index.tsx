import type { FC } from 'react';
import React from 'react';

import { ListSection } from '../components';

const Thoughts: FC = () => {
  return <ListSection title={'想法'} sectionKey={'thoughts'} />;
};

export default Thoughts;
