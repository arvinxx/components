const base = require('../../jest.config.base');

const packageName = '@arvinxu/<%- pkg %>';

const root = '<rootDir>/packages/<%- pkg %>';

module.exports = {
...base,
rootDir: '../..',
roots: [root],
name: packageName,
displayName: packageName,
};

