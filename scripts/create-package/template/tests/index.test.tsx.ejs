import React from 'react';
import { render } from '@testing-library/react';

import <%- componentName %> from '@arvinxu/<%- pkg %>';

describe('<%- componentName %>', () => {
  it('默认状态', () => {
    const { container } = render(<<%- componentName %> />);
    expect(container).toMatchSnapshot();
  });
});
